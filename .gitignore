.hlx/*
coverage/*
logs/*
node_modules/*

helix-importer-ui
.DS_Store
*.bak
.idea

# Cursor project MCP config (local only; use ~/.cursor/mcp.json for Easy MCP)
.cursor/mcp.json

# Ensure these files are tracked
!CLAUDE.md
!AGENTS.md
